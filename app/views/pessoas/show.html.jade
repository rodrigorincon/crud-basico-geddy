.hero-unit
  h2.id= pessoa.id
  != linkTo('Edit this pessoa', editPessoaPath(params.id), {class: 'btn pull-right'})
  != linkTo('Back to list', pessoaPath(''), {class: 'btn pull-right'})

h2 Pessoa Properties
each property, key in pessoa
  if key !== 'saved'
    .row.list-item
      .span4
        h3= key
      .span8
        h3
          i.icon-list-alt
          span(class=key)= property
